# Get a free access key at https://web3forms.com — set the notification email to
# ktsgeneralsolution@gmail.com in the Web3Forms dashboard.
VITE_WEB3FORMS_ACCESS_KEY=

# Optional: public site URL (no trailing slash). Netlify sets URL automatically during build;
# add this for local builds if you want canonical + og:image URLs in index.html.
# VITE_SITE_URL=https://yoursite.netlify.app
